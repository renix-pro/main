/**
 * RENIX vNext — Authentication Configuration (Client)
 * 
 * Canon v1.4 Compliant — Phase 12 Auth Addendum
 * 
 * Re-exports shared auth config for client use.
 */

export { AUTH_ENABLED, TEST_USER, AUTH_ROUTES, SESSION_CONFIG } from '@shared/authConfig';
